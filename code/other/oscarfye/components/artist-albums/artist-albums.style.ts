import styled from "styled-components";
import { Component } from "./artist-albums.component";

export const ArtistAlbums = styled(Component)`
  .title {
    font-size: 24px;
    font-weight: 700;
    line-height: 28px;
    letter-spacing: -0.04em;
    text-transform: none;
    font-family: "Montserrat", sans-serif;
    color: white;
    padding: 20px;
  }
`;
